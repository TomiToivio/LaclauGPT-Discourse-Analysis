name: Collector tests

on:
  pull_request:
    paths:
      - "collector/**"
      - "tests/collectors/**"
      - ".github/workflows/collector-tests.yml"
  push:
    branches: [main]
    paths:
      - "collector/**"
      - "tests/collectors/**"
      - ".github/workflows/collector-tests.yml"

permissions:
  contents: read

jobs:
  collector:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install collector test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r collector/requirements.txt pytest

      - name: Python collector tests
        run: python -m pytest -q tests/collectors

      - name: Firefox extension syntax
        run: |
          node --check collector/firefox/extension/capture.js
          node --check collector/firefox/extension/navigation.js
          node --check collector/firefox/extension/content.js
